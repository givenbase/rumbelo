# Generated locale files

Do not treat these as the source of truth. Edit `../translations/**/*.ts`, then:

```bash
pnpm --filter @rumbelo/i18n generate
```

`nl.json` may contain Dutch overrides; `generate` only **fills missing** keys from English.
