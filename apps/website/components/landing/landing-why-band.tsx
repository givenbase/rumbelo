export function LandingWhyBand() {
  return (
    <section className="border-b border-t border-line bg-bg-app">
      <div className="mx-auto max-w-[900px] px-[clamp(14px,3vw,22px)] py-[clamp(40px,6vw,72px)] text-center">
        <span className="font-mono text-[10.5px] font-medium uppercase tracking-[0.2em] text-accent">
          ✦ WHY RUMBELO EXISTS
        </span>
        <p className="mx-auto mt-[18px] max-w-[30ch] font-display text-[clamp(22px,3.2vw,32px)] font-semibold leading-[1.3] tracking-tight">
          Rich people don&apos;t budget harder. They give every euro a job — and then stop thinking
          about it.
        </p>
        <span className="mt-[18px] block font-mono text-[10.5px] font-medium uppercase tracking-[0.14em] text-fg-faint">
          Built in Amsterdam · PSD2 read-only · Cancel anytime
        </span>
      </div>
    </section>
  );
}
