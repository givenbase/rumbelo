/**
 * Tour / Help English copy, keyed for a later move to `@rumbelo/i18n`
 * (`features.tour.offer.*`, `features.tour.chrome.*`, `features.tour.pages.*`).
 */
export { default as offer } from './offer';
export { default as chrome } from './chrome';
export * as pages from './pages';
