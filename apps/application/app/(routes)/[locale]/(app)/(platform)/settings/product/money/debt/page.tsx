import { DebtSettings } from '../../../_components/settings-panels';

export const metadata = { title: 'Debt — Settings' };

export default function DebtSettingsPage() {
    return <DebtSettings />;
}
