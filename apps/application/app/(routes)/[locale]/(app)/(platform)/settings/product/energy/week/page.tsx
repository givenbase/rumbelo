import { EnergySettings } from '../../../_components/settings-panels';

export const metadata = { title: 'Week — Settings' };

export default function EnergyWeekSettingsPage() {
    return <EnergySettings />;
}
