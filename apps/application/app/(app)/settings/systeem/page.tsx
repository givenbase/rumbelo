import { SysteemSettings } from '../_components/settings-panels';

export const metadata = { title: 'System — Settings' };

export default function SysteemSettingsPage() {
  return <SysteemSettings />;
}
