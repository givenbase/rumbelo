export { evaluateSplitCoach, pctByJarKey } from './evaluate';
export {
    DEFAULT_JAR_SPLIT,
    MoneyCharacter,
    SPLIT_SOFT_CEILING,
    SPLIT_SOFT_FLOOR,
    type SplitPctByKey,
    type SplitTip,
    type SplitTipSeverity,
} from './types';
