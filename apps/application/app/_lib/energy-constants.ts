export const SESSION_COLORS: Record<string, string> = {
    STRENGTH: 'var(--color-jar-ff)',
    RUNNING: 'var(--color-jar-lts)',
    MOBILITY: 'var(--color-jar-edu)',
    YOGA: 'var(--color-portal-soul)',
};

/** Default hours per night for static week/sleep layout when no live data. */
export const DEFAULT_SLEEP_HOURS = 8;
